# TLS Certificates

Place your TLS certificate and key here:
- `cert.pem` -- certificate (PEM format)
- `key.pem` -- private key (PEM format)

For development, Caddy can auto-generate self-signed certificates.
For production, use certificates provided by your IT department.

These files are gitignored and must not be committed.
